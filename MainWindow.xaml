<Window x:Class="PSX.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:controls="clr-namespace:PSX.Controls"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="PSX"
        Icon="Assets/Icons/psx.ico"
        Height="800" Width="1200"
        MinHeight="560" MinWidth="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        Background="{DynamicResource WindowBackgroundBrush}">

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Width" Value="46"/>
            <Setter Property="Height" Value="48"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template">
                <Setter.Value><ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" BorderThickness="0" BorderBrush="Transparent"><ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                </ControlTemplate></Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{DynamicResource HoverBrush}"/></Trigger>
                <Trigger Property="IsKeyboardFocused" Value="True"><Setter Property="Background" Value="{DynamicResource HoverBrush}"/></Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Window.InputBindings>
        <KeyBinding Gesture="Ctrl+Shift+T" Command="{Binding NewTabCommand}"/>
        <KeyBinding Gesture="Ctrl+Shift+W" Command="{Binding CloseTabCommand}" CommandParameter="{Binding ActiveTab.SessionId}"/>
        <KeyBinding Gesture="Ctrl+Alt+Right" Command="{Binding FocusAdjacentPaneCommand}" CommandParameter="1"/>
        <KeyBinding Gesture="Ctrl+Alt+Left" Command="{Binding FocusAdjacentPaneCommand}" CommandParameter="-1"/>
        <KeyBinding Gesture="Ctrl+Alt+Enter" Command="{Binding TogglePaneZoomCommand}"/>
    </Window.InputBindings>

    <Grid x:Name="WindowContentRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Visibility="Collapsed" Background="{DynamicResource WindowBackgroundBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <controls:TabBar x:Name="WorkspaceTabBar" Grid.Column="0" DataContext="{Binding}"/>
        </Grid>

        <Border Grid.Row="1" Background="{DynamicResource WindowBackgroundBrush}">
            <controls:TerminalHost x:Name="TerminalHostControl"/>
        </Border>
        <Border x:Name="CaptionPanel" Grid.Row="1" Width="138" Height="48" HorizontalAlignment="Right" VerticalAlignment="Top"
                Background="{DynamicResource WindowChromeBrush}" BorderBrush="Transparent" BorderThickness="0">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="MinimizeCaption" Style="{StaticResource CaptionButton}" Click="MinimizeWindow">
                    <Path Data="M0,5 L10,5" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1" Width="10" Height="10"/>
                </Button>
                <Button x:Name="MaximizeCaption" Style="{StaticResource CaptionButton}" Click="ToggleMaximizeWindow">
                    <Path x:Name="MaximizeGlyph" Data="M0,0 L10,0 10,10 0,10 Z" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1" Width="10" Height="10"/>
                </Button>
                <Button x:Name="CloseCaption" Click="CloseWindow">
                    <Button.Style><Style TargetType="Button" BasedOn="{StaticResource CaptionButton}">
                        <Style.Triggers><Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#C42B1C"/><Setter Property="Foreground" Value="White"/>
                        </Trigger></Style.Triggers>
                    </Style></Button.Style>
                    <Path Data="M0,0 L10,10 M10,0 L0,10" Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}" StrokeThickness="1" Width="10" Height="10"/>
                </Button>
            </StackPanel>
        </Border>

        <!-- Status bar — populated by MainViewModel.SetStatus(...) from services.
             Collapsed to zero height when IsStatusVisible is false, so it doesn't
             eat vertical space during normal use. -->
        <Border Grid.Row="2"
                Background="{DynamicResource SurfaceMutedBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="10,4">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsStatusVisible}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="{Binding StatusMessage}"
                           ToolTip="{Binding StatusMessage}"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextSecondaryBrush}"
                           FontSize="11"
                           TextTrimming="CharacterEllipsis"/>
                <Button Grid.Column="1"
                        Style="{StaticResource SoftWorkbenchIconButtonStyle}"
                        Width="22"
                        Height="20"
                        Margin="8,0,0,0"
                        ToolTip="{Binding DismissWarningToolTip}"
                        Command="{Binding DismissPersistentWarningCommand}"
                        Visibility="{Binding HasPersistentWarning, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Path Data="M3,3 L9,9 M9,3 L3,9"
                          Stroke="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                          StrokeThickness="1.5"
                          StrokeStartLineCap="Round"
                          StrokeEndLineCap="Round"
                          Stretch="Uniform"
                          Width="9"
                          Height="9"/>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>
