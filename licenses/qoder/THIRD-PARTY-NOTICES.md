# Qoder CLI (`@qoder-ai/qodercli`) — Apache-2.0

The Qoder CLI package itself is licensed under the Apache License 2.0.
See `LICENSE` in this directory (copied from the upstream package).

PSX ships only the seed manifests under `tools/qoder-seed/`. The installed
runtime under `runtime/qoder-current/` is downloaded from the official npm
registry after explicit user confirmation and is not redistributed inside the
public ZIP. Upstream production dependencies retain their own licenses inside
the downloaded package tree.
