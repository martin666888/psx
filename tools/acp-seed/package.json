{
  "private": true,
  "name": "psx-acp-runtime",
  "version": "0.1.0",
  "description": "PSX ACP adapter runtime — installed into the portable PSX runtime directory after user confirmation, then refreshed in the background.",
  "dependencies": {
    "@agentclientprotocol/claude-agent-acp": "0.57.0"
  }
}
